import { Typography } from "@mui/material";
import DrawerComponent from "../componentes/DrawerComponent";

export default function Dashboard() {
  return (
    <DrawerComponent title="Página inicial">
      <Typography>Olá</Typography>
    </DrawerComponent>
  );
}
